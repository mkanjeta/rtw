name: Build Full FFmpeg AAR (Latest + 16KB)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install -y yasm nasm unzip git build-essential pkg-config automake autoconf libtool openjdk-17-jdk zip

    - name: Download Android NDK r26d
      run: |
        mkdir -p $HOME/ndk
        wget -q https://dl.google.com/android/repository/android-ndk-r26d-linux.zip
        unzip -q android-ndk-r26d-linux.zip
        mv android-ndk-r26d/* $HOME/ndk/

    - name: Download FFmpeg latest
      run: |
        git clone https://github.com/FFmpeg/FFmpeg ffmpeg-src --depth=1

    - name: Build FFmpeg arm64-v8a
      run: |
        export NDK=$HOME/ndk
        export TOOLCHAIN=$NDK/toolchains/llvm/prebuilt/linux-x86_64
        export API=21

        export CC="$TOOLCHAIN/bin/aarch64-linux-android${API}-clang"
        export CXX="$TOOLCHAIN/bin/aarch64-linux-android${API}-clang++"
        export AR="$TOOLCHAIN/bin/aarch64-linux-android-ar"
        export RANLIB="$TOOLCHAIN/bin/aarch64-linux-android-ranlib"

        mkdir -p build-arm64
        cp -r ffmpeg-src build-arm64/ffmpeg
        cd build-arm64/ffmpeg

        ./configure \
          --prefix=$HOME/output/arm64 \
          --target-os=android \
          --arch=aarch64 \
          --enable-cross-compile \
          --cc="$CC" \
          --enable-shared \
          --disable-static \
          --disable-programs \
          --disable-doc \
          --disable-stripping \
          --extra-cflags="-fPIC" \
          --extra-ldflags="-Wl,-z,max-page-size=16384"

        make -j$(nproc)
        make install

    - name: Create AAR structure
      run: |
        mkdir -p aar/jni/arm64-v8a
        mkdir -p aar/java/com/example/ffmpeg
        mkdir -p aar/META-INF
        mkdir -p jni

        cp $HOME/output/arm64/lib/*.so aar/jni/arm64-v8a/

        # Manifest
        echo '<manifest package="com.example.ffmpeg" />' > aar/AndroidManifest.xml

        # Java API
        cat > aar/java/com/example/ffmpeg/FFmpeg.java <<'EOF'
package com.example.ffmpeg;

public class FFmpeg {
    static {
        System.loadLibrary("avutil");
        System.loadLibrary("swresample");
        System.loadLibrary("swscale");
        System.loadLibrary("avcodec");
        System.loadLibrary("avformat");
        System.loadLibrary("ffmpegjni");
    }

    public static native int execute(String[] argv);
}
EOF

        # JNI Code (safe version — no strdup)
        cat > jni/ffmpeg_jni.c <<'EOF'
#include <jni.h>
#include <stdlib.h>
#include <string.h>

extern int ffmpeg_execute(int argc, char **argv);

static char* my_strdup(const char* s) {
    size_t len = strlen(s) + 1;
    char* p = (char*)malloc(len);
    if (p) memcpy(p, s, len);
    return p;
}

JNIEXPORT jint JNICALL
Java_com_example_ffmpeg_FFmpeg_execute(JNIEnv* env, jclass clazz, jobjectArray args) {
    int argc = (*env)->GetArrayLength(env, args);
    char** argv = (char**)malloc(sizeof(char*) * argc);

    for (int i = 0; i < argc; i++) {
        jstring jstr = (jstring)(*env)->GetObjectArrayElement(env, args, i);
        const char* raw = (*env)->GetStringUTFChars(env, jstr, 0);
        argv[i] = my_strdup(raw);
        (*env)->ReleaseStringUTFChars(env, jstr, raw);
    }

    int result = ffmpeg_execute(argc, argv);

    for (int i = 0; i < argc; i++) free(argv[i]);
    free(argv);

    return result;
}
EOF

        # Build JNI shared lib
        $HOME/ndk/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android21-clang \
          -shared jni/ffmpeg_jni.c \
          -o aar/jni/arm64-v8a/libffmpegjni.so \
          -fPIC

        # Compile Java → classes.jar
        mkdir -p aar/classes
        javac -source 8 -target 8 -d aar/classes aar/java/com/example/ffmpeg/FFmpeg.java
        cd aar/classes
        zip -r ../classes.jar .
        cd ../..

        # Build final AAR
        cd aar
        zip -r ../ffmpeg-full-16kb.aar .
        cd ..

    - name: Upload AAR
      uses: actions/upload-artifact@v4
      with:
        name: full-ffmpeg-16kb-aar
        path: ffmpeg-full-16kb.aar
